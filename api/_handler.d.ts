// Type surface for the generated bundle `api/_handler.js` (produced by scripts/bundle-server.mjs).
// Re-exports the real types from the source so the API functions stay fully typed while
// importing the runtime values from the single-file bundle.
export { healthPayload, runCopilot } from "../server/handler";
export type { CopilotBody, HandlerResult } from "../server/handler";
